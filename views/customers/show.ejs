<%- include('../partials/header') %>
<body class="show-page"></body>
<section>
  <center>
  <table class="table my-5 table-rounded rounded-4 overflow-hidden" style="border-collapse: collapse; border: solid black 1px; border-radius: 10px; width: 50%;">
    <thead>
      <tr>
        <th>Name</th>
        <th>Address</th>
        <th>Phone</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= customer[0].name %></td>
        <td><%= customer[0].address %></td>
        <td><%= customer[0].phone %></td>
        <td><a class="btn btn-primary" href="/customers/<%= customer[0].id %>/edit">Edit</a></td>
      </tr>
    </tbody>
  </table>
        <table class="table table-rounded rounded-4 overflow-hidden w-50">
            <thead>
              <tr>
                <th>Name</th>
                <th>Date of Birth</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% if (customer[0].cats) { %>
                <% customer[0].cats.forEach(function(cat) { %>
                  <tr>
                    <td><%= cat.name %></td>
                    <td><%= cat.dob %></td>
                    <td>
                      <form action="/cats/<%= customer[0].id %>/<%= cat.id %>?_method=DELETE" method="POST">
                        <button type="submit" class="btn btn-danger">Delete</button>
                      </form>
                    </td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="3">No cats yet.</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </center>

        <!-- <form action="/cats/<%= customer[0].id %>" method="POST">
            Name: <input type="text" name="name">
            Date of Birth: <input type="date" name="dob">
            <button type="submit">Add Cat</button>
        </form> -->

        <form action="/cats/<%= customer[0].id %>" method="POST">
          <div class="row">
            <div class="col-sm-3">
              <div class="form-floating mb-3">
                <input type="text" name="name" class="form-control" id="name" placeholder=" ">
                <label for="name">Name</label>
              </div>
            </div>
            <div class="col-sm-3">
              <div class="form-floating mb-3">
                <input type="date" name="dob" class="form-control" id="dob" placeholder=" ">
                <label for="dob">Date of Birth</label>
              </div>
            </div>
          </div>
          <button type="submit" class="btn btn-primary">Add Cat</button>
        </form>
        

        
            
</section>

<%- include('../partials/footer') %>